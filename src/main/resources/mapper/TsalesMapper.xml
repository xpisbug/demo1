<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.smdemo1.dao.TsalesMapper">
	<insert id="insert" parameterType="com.example.smdemo1.model.Tsales">
		/*insert into user(`id`, `name`, `age`, `desc`)value(
			#{id},#{name},#{age},#{desc}
		)*/
	</insert>

	<select id="listAll" resultType="com.example.smdemo1.model.Tsales">
		select * from t_sales where id > 0
	</select>
</mapper>